<UserControl x:Class="CarDealerApp.Views.VehiclesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:CarDealerApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:VehiclesViewModel/>
    </UserControl.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Gerenciamento de Veículos" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Veiculos}"
                  SelectedItem="{Binding SelectedVeiculo}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Marca" Binding="{Binding Marca}" Width="*"/>
                <DataGridTextColumn Header="Modelo" Binding="{Binding Modelo}" Width="*"/>
                <DataGridTextColumn Header="Ano" Binding="{Binding AnoModelo}" Width="Auto"/>
                <!-- CORREÇÃO: Removida a propriedade 'Culture' inválida -->
                <DataGridTextColumn Header="Valor" Binding="{Binding Valor, StringFormat=C}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Adicionar" Width="100" Margin="5" Command="{Binding AddVehicleCommand}"/>
            <Button Content="Editar" Width="100" Margin="5" Command="{Binding EditVehicleCommand}"/>
            <Button Content="Excluir" Width="100" Margin="5" Command="{Binding DeleteVehicleCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
