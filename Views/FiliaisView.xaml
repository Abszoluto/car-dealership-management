<UserControl x:Class="CarDealerApp.Views.FiliaisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:CarDealerApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- Define o ViewModel para esta View -->
    <UserControl.DataContext>
        <vm:FiliaisViewModel/>
    </UserControl.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Gerenciamento de Filiais" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Filiais}"
                  SelectedItem="{Binding SelectedFilial}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdFilial}" Width="Auto"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="CNPJ" Binding="{Binding Cnpj}" Width="*"/>
                <DataGridTextColumn Header="Cidade" Binding="{Binding Cidade}" Width="*"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="Auto"/>
                <DataGridTextColumn Header="Data de Criação" Binding="{Binding DataCriacao}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Adicionar" Width="100" Margin="5" Command="{Binding AddFilialCommand}"/>
            <Button Content="Editar" Width="100" Margin="5" Command="{Binding EditFilialCommand}"/>
            <Button Content="Excluir" Width="100" Margin="5" Command="{Binding DeleteFilialCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>