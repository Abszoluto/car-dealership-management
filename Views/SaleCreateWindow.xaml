<Window x:Class="CarDealerApp.Views.SaleCreateWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    Title="Nova Venda" Height="320" Width="500">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical">
            <Label>Cliente</Label>
            <ComboBox x:Name="ClienteCombo" DisplayMemberPath="NomeRazao" SelectedValuePath="IdCliente" />
            <Label>Veículo</Label>
            <ComboBox x:Name="VeiculoCombo" DisplayMemberPath="Modelo" SelectedValuePath="IdVeiculo" />
            <Label>Data Venda</Label>
            <DatePicker x:Name="DataVenda" />
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Width="80" Margin="4" IsDefault="True" Click="Ok_Click">OK</Button>
            <Button Width="80" Margin="4" IsCancel="True">Cancelar</Button>
        </StackPanel>
    </Grid>
</Window>
